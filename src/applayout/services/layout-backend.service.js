(function () {
    'use strict';
    angular.module('glance.layout')
        .factory('layoutBackend', layoutBackend);


    /* @ngInject */
    function layoutBackend(gHttp) {
        return {

        };
    }
})();