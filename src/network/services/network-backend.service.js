(function () {
    'use strict';
    angular.module('app.network')
        .factory('networkBackend', networkBackend);


    /* @ngInject */
    function networkBackend(gHttp) {
        return {

        };
    }
})();