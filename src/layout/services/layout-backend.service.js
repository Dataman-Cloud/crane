(function () {
    'use strict';

    angular.module('app.layout').factory('layoutBackend', layoutBackend);

    /* @ngInject */
    function layoutBackend(gHttp) {

        return {

        };
    }
})();